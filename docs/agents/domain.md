# Domain Docs

This is a single-context repository.

Before exploring relevant work, read the root `CONTEXT.md` and any applicable decisions in `docs/adr/`. Use the glossary's canonical terms in issues, specifications, and tests. If a needed domain concept is missing, resolve it through the domain-modeling workflow rather than adding implementation details to `CONTEXT.md`.
